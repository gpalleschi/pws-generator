import React from 'react'
import ReactDOM from 'react-dom/client';
import { PWSApp } from './PWSApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <PWSApp />
);

